# walgreens-rag-demo
This is a minimal RAG (retrieval-augmented generation) demo in Python using LangChain. It loads a local text file, chunks it, builds a FAISS vector index, retrieves the most relevant chunks for a question, and then answers using an OpenAI model.
